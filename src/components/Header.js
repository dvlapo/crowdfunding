import Navbar from './Navbar';

const Header = () => {
  return (
    <header className="bg-hero-bg-mobile sm:bg-hero-bg-desktop bg-cover">
      <Navbar />
    </header>
  );
};

export default Header;
